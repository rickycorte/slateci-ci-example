language: ruby

rvm:
  - 2.4.0

cache: bundler

before_install:
  - ls
  - git clone https://github.com/lord/slate.git
  - mv -f docs/* slate/source/
  - cd slate

script:
  - bundle exec middleman build
  - ls